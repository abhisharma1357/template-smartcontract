Use this repository as a template to build smart contracts with security tools 


use `npm install --save`

use `bash test.sh` to run truffle test cases 

use `bash tool.sh` to run truffle tools  

prerequisite :

node, npm, docker


